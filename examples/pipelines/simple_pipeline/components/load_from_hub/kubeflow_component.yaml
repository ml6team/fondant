name: load_from_hub
description: A component that takes a dataset name from the 🤗 hub as input and uploads it to a GCS bucket.
inputs: 
    - name: input_manifest_path
      description: Path to the input manifest
      type: String
    
    - name: args
      description: Additional arguments passed to the component, as a json dict string
      type: String
    
    - name: metadata
      description: Metadata arguments, passed as a json dict string
      type: String

    - name: spec_path
      description: Path to the Fondant component spec
      type: String
    
    
outputs:
    - name: output_manifest_path
      description: Path to the output manifest

implementation:
    container:
        image: ghcr.io/ml6team/load_from_hub:latest
        command: [
            python3, main.py,
            --input-manifest-path,       {inputPath: input_manifest_path},
            --args,                      {inputValue: args},
            --metadata,                  {inputValue: metadata},
            --spec-path,                 {inputValue: spec_path},
            --output-manifest-path,      {outputPath: output_manifest_path},
        ]