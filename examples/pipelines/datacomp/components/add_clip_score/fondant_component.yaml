name: Add CLIP score
description: Component that adds the CLIP score
image: fndnt/add_clip_score:0.6.2

consumes:
  embeddings:
    fields:
      data:
        type: array
        items:
          type: float32
  textembedding:
    fields:
      data:
        type: array
        items:
          type: float32

produces:
  imagetext:
    fields:
      clipl14score:
        type: float32