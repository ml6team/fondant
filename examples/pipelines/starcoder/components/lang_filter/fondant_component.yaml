# TODO: Rename (also image)
name: lang filter
description: Component that filters the data based on a specific language
# TODO: update image location
image: localhost:5000/lang_filter:1

consumes:
  code:
    fields:
      lang:
        type: string
args:
  lang:
    description: lang to filter on
    type: str
