name: Example component
description: This is an example component
image: example_component:latest

input_subsets:
  images:
    fields:
      data:
        type: binary

output_subsets:
  captions:
    fields:
      data:
        type: string