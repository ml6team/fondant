name: check_gpu_component
description: A component that checks the utilization of GPU.

inputs: 
    - name: project_id
      description: The id of the gcp-project
      type: String

implementation:
    container:
        image: europe-west1-docker.pkg.dev/storied-landing-366912/storied-landing-366912-default-repository/mlpipelines/kubeflow/components/check_gpu_component:latest
        command: [
            python3, main.py,
            --project-id,       {inputValue: project_id}
        ]