name: Depth processor
description: Component that creates a depth map for input images
image: ghcr.io/ml6team/depth_processor:latest

consumes:
  images:
    fields:
      data:
        type: binary

produces:
  depth:
    fields:
      data:
        type: binary
