name: Canny edge processor
description: Component that creates a canny edge map for input images
image: ghcr.io/ml6team/canny_edge_processor:latest

consumes:
  images:
    fields:
      data:
        type: binary

produces:
  depth:
    fields:
      data:
        type: binary
