name: HED processor
description: Component that creates a HED map for input images
image: ghcr.io/ml6team/hed_processor:latest

consumes:
  images:
    fields:
      data:
        type: binary

produces:
  hed:
    fields:
      data:
        type: binary
