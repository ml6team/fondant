name: Filter text length
description: A component that filters out text based on their length
image: ghcr.io/ml6team/filter_text_length:latest

consumes:
  text:
    fields:
      data:
        type: string

args:
  min_length:
    description: Min length of the text passage
    type: int