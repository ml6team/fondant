name: MLSD processor
description: Component that creates a MLSD map for input images
image: ghcr.io/ml6team/mlsd_processor:latest

consumes:
  images:
    fields:
      data:
        type: binary

produces:
  mlsd:
    fields:
      data:
        type: binary
