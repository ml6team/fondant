name: Example component
description: This is an example component
image: example_component:latest

accepts:
  images:
    data: binary

produces:
  captions:
    data: utf8

args:
  # Additional arguments accepted by the component